Each Section is its own form. 
All Form data is collected in a central hub called FormContext under the Typescript Interface ApplicantInfo.
    Interface AppliccntInfo {
        Section1: Section1FormInterface
        Section2: Section2FormInterface
        Section3: Section3FormInterface[]
        ...
    }

I need to build a few different reusable components.

Compoenent1 needs to be a conditionally triggered compoeoent based on other values within the parent form.
Compoenent1 will be used in Section5 when a box value is checked so it is true, Another section of data containing components similar to or exactly like Component2 or Component3. 

Component2 is a simple Generic input field similar to the example below, but needs adjustments:
